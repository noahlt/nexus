Dependencies:
	python-django >= 1.0
	python-imaging (PIL)
	python-simplejson
	python-markdown
	python-pysqlite2 (or similar if not using sqlite)
	evince OR python-magic (evince faster)
	pdftk OR python-pypdf (pdftk faster)
