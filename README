Dependencies:

  python-django >= 1.0
  python-imaging (PIL)
  python-simplejson
  python-markdown
  evince OR imagemagick (evince faster)
  pdftk OR python-pypdf (both sufficiently fast; pdftk faster)

  psycopg (if using postgresql)

  django_evolution (optional)
